import { propagateServerField } from 'next/dist/server/lib/render-server';

export const metadata = {
  title: 'Next.js',
  description: 'Generated by Next.js',
};
// Layout.jsは削除しても自動で生成される
// propsでpage.tsxをchildrenとして受け取って表示するように作られているためpage.tsxに読み込ませた下層ページにも共通したパーツを表示させることができる
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        <header>
          <p>ヘッダーです</p>
        </header>
        {children}
        <footer>
          <p>フッターです</p>
        </footer>
      </body>
    </html>
  );
}
